<x-layouts.public>
    <section class="max-w-7xl mx-auto px-4 py-24 text-center">
        <h1 class="text-4xl font-bold text-brand-700">{{ config('site.name') }}</h1>
        <p class="mt-4 text-muted">{{ config('site.tagline') }}</p>
        <p class="mt-2 text-sm text-gray-400">Layout publik aktif. Phase C akan mengisi section lengkap.</p>
    </section>
</x-layouts.public>
